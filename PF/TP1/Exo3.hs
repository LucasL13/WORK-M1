n = a `div`  length  xs
    where
        a = 10
        xs = [1,2,3,4,5]

-- n doit être écrit en minuscule -- 
-- les lignes liées au "where" doivent être au meme niveau de tabulation (tab(where)+1) --
